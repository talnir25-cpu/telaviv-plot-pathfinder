## תכנית שינוי סדר שדות בשלב 2

### מבנה נוכחי (שלב 2 — המצב הקיים):
1. יח"ד קיימות
2. קומות קיימות
3. שטח בנוי קיים (md:col-span=2 — תופש שורה שלמה)
4. שנת בנייה

### מבנה רצוי:
1. **שטח בנוי קיים** (md:col-span=1 — חצי שורה)
2. **שנת בנייה** (md:col-span=1 — חצי שורה, באותה שורה עם שטח בנוי)
3. יח"ד קיימות
4. קומות קיימות

### פעולות:
- הסר את `md:col-span-2` משטח בנוי קיים
- הסר את פסקת ההסבר מתחת לשטח בנוי (היא תופשת רוחב, ומונעת שתי עמודות בשורה)
- העבר את בלוק שטח בנוי + בלוק שנת בנייה לפני יח"ד קיימות וקומות קיימות
- שמור על כל ה-logic וה-badges הקיימים

### קובץ:
- `src/components/PlotPicker.tsx` — בלוק שלב 2 בטופס